# Extraire un fichier tar.gz

```bash
tar -xvzf nom_du_fichier.tar.gz
```

**Description** : Extrait le contenu d'un fichier tar.gz. Les options sont : x (extraire)
**Tags** :  v (verbose), z (gzip), f (fichier).,linux,tar,extract
