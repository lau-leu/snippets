# Ajouter un utilisateur à un groupe

```bash
sudo usermod -a -G www-local laurent
```

**Description** : Ajoute l'utilisateur 'laurent' au groupe 'www-local'.
**Tags** : linux,usermod,user,group,add
