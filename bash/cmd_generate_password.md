# Générer un mot de passe aléatoire

```bash
gpg --gen-random --armor 1 16
```

**Description** : Génère un mot de passe aléatoire de 16 caractères sans caractères spéciaux.
**Tags** : linux,gpg,password,generate
