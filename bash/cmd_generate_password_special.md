# Générer un mot de passe aléatoire avec caractères spéciaux

```bash
pwgen -ys 16 1
```

**Description** : Génère un mot de passe aléatoire de 16 caractères avec caractères spéciaux.
**Tags** : linux,pwgen,password,generate
