# Mettre à jour et nettoyer les paquets

```bash
sudo apt update && sudo apt full-upgrade && sudo apt autoremove
```

**Description** : Met à jour la liste des paquets
**Tags** :  effectue une mise à jour complète et supprime les paquets inutiles.,linux,apt,update,clean
